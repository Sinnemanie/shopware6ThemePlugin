{% sw_extends '@Storefront/storefront/component/product/listing.html.twig' %}

{#{% block product_listing %}
    <h1>fklsjfljsl</h1>
    {{ parent() }}
{% endblock %}#}

{% block element_product_listing_row %}

        <div class="row">
                {% for product in searchResult %}
            <div class="col-12 col-md-6 col-lg-4">
                    <div class="card training-card">
                        <div class="img-container">
                            <img class="img-fluid img-thumbnail rounded" src="{{ product.cover.media.url }}" alt="{{ product.name }}">
                        </div>
                        <div class="card-body">
                            <h5 class="card-title">{{ product.translated.name }}</h5>
                            <p class="card-text">
                                {{ product.translated.description }}
                            </p>
                            <p class="card-text">
                                {# {{ dump(product.price) }} #}
                                {% sw_include '@Storefront/storefront/component/product/card/price-unit.html.twig' %}
                            </p>
                            <a href="{{ path('frontend.detail.page', {'productId': product.id }) }}" class="btn btn-primary btn-block">Details</a>
                        </div>
                    </div>
            </div>
                {% endfor %}
        </div>
{% endblock %}
